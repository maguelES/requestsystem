databaseChangeLog:
       - preConditions:
                - onFail: HALT
                - onError: HALT
       - includeAll:
                path: changelog-1.x
                relativeToChangelogFile: true
              
